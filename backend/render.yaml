services:
  - type: web
    name: procurement-work-ai-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 10000
    plan: free
    envVars:
      - key: ENVIRONMENT
        value: production
